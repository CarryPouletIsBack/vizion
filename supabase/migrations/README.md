# Migrations Supabase

Exécuter les migrations dans l’ordre (par nom de fichier) si besoin.

**Méthode recommandée :** Supabase Dashboard → **SQL Editor** → coller le contenu du fichier `.sql` → Run.

- `20250209000000_add_course_date_start_time.sql` : ajoute les colonnes `date` et `start_time` à la table `courses` (départ officiel + simulation).
